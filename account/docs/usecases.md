```mermaid
flowchart LR
    Client[Client / Frontend]

    Client -->|Register User| RegisterUser
    Client -->|Get User| GetUser
    Client -->|List User Accounts| GetUserAccounts
    Client -->|Get Account| GetAccount
```
